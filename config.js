// Fill these in from Supabase: Project settings -> Data API (and API keys).
// The anon key is meant to be public. It grants nothing on its own here —
// every read and write also needs a collection code.
const CONFIG = {
  url:     "https://YOUR-PROJECT-REF.supabase.co",
  anonKey: "YOUR-ANON-KEY"
};
